# Periscope Phase 2 Demo Fixture

The builder creates the OAuth implementation and tests. It also creates `infra/prod.tf` intentionally so intent comparison and review have a deterministic finding to resolve.
